<button class="btn btn-sm btn-info btn-edit" data-id="{{ $row->id }}" data-year="{{ $row->year }}" data-semester="{{ $row->semester }}" data-start="{{ $row->start_date }}" data-mid="{{ $row->mid_date }}" data-end="{{ $row->end_date }}">
    Edit
</button>
<button class="btn btn-sm btn-danger btn-delete" data-id="{{ $row->id }}">
    Hapus
</button>
